function breakOut(arr, cVal, sVal) {
  arr.forEach(elem => {
    if (elem === sVal) {
      break;
    }

    elem = cVal
  })
}

function keepGoing(arr, cVal, sVal) {
  
}

function findBy(arr, findFn)